{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for comp sci NEA project",
  "main": "index.js",
  "scripts": {
    "test": "tsc && cd build && node main.js && cd .."
  },
  "author": "Oscar Ratcliffe",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.7"
  }
}
